{
  "name": "omo-claw",
  "version": "0.1.0",
  "description": "SDK-only OpenClaw plugin and context-engine bridge for OpenCode + OmO",
  "private": true,
  "type": "module",
  "openclaw": {
    "extensions": [
      "./src/openclaw-bridge-plugin.ts"
    ]
  },
  "bin": {
    "omo-claw-install": "./scripts/omo-claw-install.mjs"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Her-xanadu/omo-claw.git"
  },
  "license": "MIT",
  "scripts": {
    "compile:definitions": "bun run definitions/compiler/cli.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.9.3"
  }
}
